
const API_URL = "http://localhost:3000";

const QUICKBILL = "QUICKBILL";
const DINEIN = "DINEIN";
const PICKUP = "PICKUP";
var ORDERTYPE = QUICKBILL;

let POS_INFO;


// start → in-kitchen → ready → settle

// start → cancel

// start → in-kitchen → update → ready → settle → refund